  {
  "//": "Secrets (connection string, JWT key, admin password) are supplied by user-secrets in development and environment variables in production. See backend/README.md.",
  "ConnectionStrings": {
    "DefaultConnection": "Server=.\\SQLEXPRESS;Database=LeadMineDb;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=True"
  },
  "Jwt": {
    "Key": "",
    "Issuer": "LeadMine.Api",
    "Audience": "LeadMine.Client",
    "AccessTokenMinutes": 15,
    "RefreshTokenDays": 7
  },
  "Seed": {
    "AdminEmail": "admin@leadmine.local",
    "AdminPassword": ""
  },
  "Swagger": {
    "Enabled": true,
    "RoutePrefix": "swagger",
    "//RequireAuthentication": "Outside Development the UI and JSON need HTTP Basic credentials. Set Swagger:Username and Swagger:Password (env vars or user-secrets). With no credentials configured the endpoint returns 404 rather than publishing the API contract. Set this to false only if the contract is meant to be public.",
    "RequireAuthentication": true,
    "Username": "",
    "Password": "",
    "//Servers": "Public base URLs advertised in the document. Required behind a reverse proxy so 'Try it out' targets the right host. Leave empty to use the request's own host.",
    "Servers": []
  },
  "Scraper": {
    "//": "The scraper runs inside this API — there is no separate worker process. GoogleApiKey is a secret: set Scraper__GoogleApiKey as an environment variable or via user-secrets. Leave it empty to run on OpenStreetMap, which needs no key.",
    "WorkerEnabled": true,
    "//Concurrency": "Jobs claimed and run at once, across every kind (search, LinkedIn enrichment, LinkedIn people search all share one queue) — with 1, a second and third queued job of a different kind sit idle behind whichever one a lone worker slot picked up first, even though nothing about them actually conflicts. 4 covers today's three job kinds plus one spare. This process also serves HTTP, so raise it only on an instance sized for it.",
    "Concurrency": 4,
    "IdlePollSeconds": 5,
    "//LeaseSeconds": "How long a claim holds without a heartbeat. Shorter recovers faster after an app-pool recycle; longer tolerates a slow task.",
    "LeaseSeconds": 120,
    "EnrichmentConcurrency": 4,
    "DelayMs": 400,
    "RateLimitPerMinute": 120,
    "RetryAttempts": 3,
    "RequestTimeoutMs": 15000,
    "MaxPagesPerSite": 4,
    "//RespectRobotsTxt": "Leave on unless you own the sites being crawled.",
    "RespectRobotsTxt": true,
    "//CrawlerContactEmail": "Advertised in the crawler's User-Agent so site owners can reach you.",
    "CrawlerContactEmail": "",
    "SaveBatchSize": 25,
    "GoogleApiKey": "",
    "//PlaywrightBrowsersPath": "Keep this short — Chromium's own nested install path can exceed Windows' 260-character path limit. See the doc comment on ScraperOptions.PlaywrightBrowsersPath.",
    "PlaywrightBrowsersPath": "App_Data/playwright-browsers",
    "//PlaywrightConcurrency": "Browser CONTEXTS (isolated profiles on one shared Chromium process), not browser instances — tens of MB each, not the 300-500MB a full instance costs. Must exceed Concurrency, since a browser search holds its context for that whole search; set below that and surplus jobs get claimed then block waiting for a context, which looks exactly like a stuck queue. 12 leaves room for Concurrency (4) plus parallel enrichment lanes.",
    "PlaywrightConcurrency": 12,
    "LinkedInStorageStatePath": "App_Data/linkedin-session.json",
    "LinkedInMaxSearchesPerDay": 80
  },
  "EmailValidation": {
    "//": "Continuous email validation for saved leads (normalize/syntax/DNS/MX/disposable/role-account), always on. BounceCheckUsername/BounceCheckAppPassword are secrets: set EmailValidation__BounceCheckUsername / EmailValidation__BounceCheckAppPassword as environment variables or via user-secrets rather than editing this file.",
    "Enabled": true,
    "PollSeconds": 1,
    "BatchSize": 10,
    "RevalidateAfterDays": 30,
    "//EnableSmtpProbe": "Off by default — see EmailVerifier's own remarks on why an RCPT TO probe is normally skipped (providers lie about it, catch-all domains defeat it, probing at volume risks the sending IP being blacklisted). Rate-limited per mail domain per hour when on.",
    "EnableSmtpProbe": false,
    "SmtpProbePollSeconds": 20,
    "SmtpProbeBatchSize": 3,
    "SmtpProbeTimeoutMs": 8000,
    "SmtpProbeMaxPerHourPerDomain": 15,
    "//EnableBounceCheck": "Off by default — sends a real probe email from BounceCheckUsername to confirm deliverability, then watches that mailbox for bounces. Capped well under Gmail's ~500/day consumer sending limit even when enabled; raise the caps only with real headroom in mind.",
    "EnableBounceCheck": false,
    "BounceCheckPollSeconds": 180,
    "BounceCheckMaxPerDay": 400,
    "BounceCheckMaxPerHour": 40,
    "BounceCheckWaitHours": 48,
    "BounceCheckMinConfidence": 50,
    "BounceCheckSmtpHost": "smtp.gmail.com",
    "BounceCheckSmtpPort": 587,
    "BounceCheckImapHost": "imap.gmail.com",
    "BounceCheckImapPort": 993,
    "BounceCheckUsername": "",
    "BounceCheckAppPassword": ""
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
      }
    }
  },
  "AllowedHosts": "*"
}
